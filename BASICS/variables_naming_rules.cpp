#include<iostream>
using namespace std;
int main(){
    int x = 5;
    cout<<x<<endl;
    // if for char and other datatypes cannot be used as an int name unless any character of them is in capital letter
}